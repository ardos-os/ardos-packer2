--- a/pkgs/development/compilers/rust/default.nix
+++ b/pkgs/development/compilers/rust/default.nix
@@ -37,9 +37,9 @@
       pkgsTargetTarget
       ;
   };
-  # Allow faster cross compiler generation by reusing Build artifacts
-  fastCross =
-    (stdenv.buildPlatform == stdenv.hostPlatform) && (stdenv.hostPlatform != stdenv.targetPlatform);
+  # Allow faster cross compiler generation by reusing Build artifacts only when ardos is not involved
+  # compiler which is unsound when the Ardos target is unknown to stage0.
+  fastCross = (stdenv.buildPlatform == stdenv.hostPlatform) && (stdenv.hostPlatform != stdenv.targetPlatform) && !(lib.strings.hasSuffix "ardos-linux-gnu" stdenv.targetPlatform.config);
 in
 {
   lib = lib';
